import 'dart:async';

import 'package:flutter/services.dart';

class clipBoardManager{
  /*
  static Future<String> getData(String format) async {
    final Map<String, dynamic> result = await SystemChannels.platform.invokeMethod(
      'Clipboard.getData',
      format,
    );
    if (result == null)
      return null;
    //return ClipboardData(text: result['text'] as String);
    return result['text'] as String;
  }

  repeat() async{
    String clip=await getData('text/plain');
    print(clip);
  }
 */

}